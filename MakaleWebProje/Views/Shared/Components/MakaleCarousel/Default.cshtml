@* @model IEnumerable<Makale>


<div class="display-6 text-center">
    <h6>ShowCase</h6>

</div>
<div class="row">

    @foreach (var mak in Model)
    {
        <partial name="_MakaleCard" model="mak" />
    }
</div> *@
@{
    ViewData["Title"] = "Home Page";
}


<div class="container mt-5 animate__animated animate__slideInLeft">
    <div id="carouselExample" class="carousel slide mb-5" data-bs-ride="carousel">
        <div class="carousel-inner">
            @{
                var isFirst = true;
            }
            @foreach (var mak in Model)
            {
                <div class="carousel-item @(isFirst ? "active" : "")">
                    <div class="d-flex justify-content-center align-items-center" style="height: 400px; background-color: #f8f9fa; border-radius: 10px;">
                        <div class="text-center">
                            <img src="@mak.MakaleImagesUrl" class="card-img-top" alt="@mak.MakaleName"
                                 style="height: 200px; object-fit: cover;">
                            <h5>@mak.MakaleName</h5>
                            <p>@mak.MakaleSummary</p>
                            <a href="Makale/get/@mak.MakaleId" class="btn btn-primary mt-3">
                                <i class="bi bi-book-half"></i> Detayları Gör
                            </a>
                        </div>
                    </div>
                </div>
                isFirst = false;
            }
        </div>

        <!-- Kontroller -->
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Önceki</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Sonraki</span>
        </button>
    </div>

    <!-- ShowCase Bölümü -->
  @*   <div class="showcase-section mt-5">
        <h2 class="text-center mb-4">Öne Çıkan Makaleler</h2>
        <div class="row g-4">
            @foreach (var mak in Model)
            {
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm">
                        <img src="@mak.MakaleImagesUrl" class="card-img-top" alt="@mak.MakaleName"
                             style="height: 200px; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title">@mak.MakaleName</h5>
                            <p class="card-text">@(mak.MakaleSummary?.Length > 100 ? mak.MakaleSummary.Substring(0, 100) + "..." : mak.MakaleSummary)</p>
                            <a href="Makale/get/@mak.MakaleId" class="btn btn-outline-primary">
                                Devamını Oku
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div> *@
</div>

<style>
    .carousel-item {
        transition: transform 0.6s ease-in-out;
    }

    .card {
        transition: all 0.3s ease;
        border: none;
        border-radius: 10px;
        overflow: hidden;
    }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 .5rem 1rem rgba(0,0,0,.15) !important;
        }

    .carousel-control-prev,
    .carousel-control-next {
        width: 5%;
        background-color: rgba(0,0,0,0.2);
        border-radius: 50%;
        height: 50px;
        top: 50%;
        transform: translateY(-50%);
    }

    .carousel-control-prev {
        left: 20px;
    }

    .carousel-control-next {
        right: 20px;
    }

    .showcase-section {
        animation: fadeIn 1s ease-in;
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style> 